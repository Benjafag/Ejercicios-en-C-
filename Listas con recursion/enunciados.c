/*
Ejercicios de Listas Enlazadas en C++

1. Crear una lista enlazada simple:
   - Define una estructura Nodo con un campo "dato" y un puntero al siguiente nodo.
   - Implementa una función para agregar nodos al final de la lista.
   - Implementa una función para imprimir la lista.

2. Insertar un nodo en una posición específica:
   - Pide al usuario una posición y un valor, e inserta el nodo en la posición correcta de la lista.

3. Eliminar un nodo por valor:
   - Permite eliminar un nodo de la lista según el valor ingresado por el usuario.

4. Buscar un elemento en la lista:
   - Implementa una función que devuelva si un número está en la lista o no.

5. Invertir la lista enlazada:
   - Implementa una función para invertir los punteros de la lista y devolver la lista en orden inverso.

6. Eliminar nodos duplicados:
   - Si la lista contiene valores repetidos, elimina los nodos duplicados dejando solo una copia de cada número.

7. Ordenar la lista enlazada:
   - Implementa un algoritmo de ordenamiento (por ejemplo, inserción o burbuja) para ordenar la lista.

8. Dividir la lista en pares e impares:
   - Separa la lista en dos listas: una con los números pares y otra con los impares.

9. Detectar ciclos en la lista:
   - Usa el algoritmo de Floyd (tortuga y liebre) para detectar si la lista tiene un ciclo.

10. Merge de dos listas ordenadas:
    - Dados dos listas enlazadas ordenadas, fusiónalas en una nueva lista ordenada.

11. Convertir una lista en una lista doblemente enlazada:
    - Transforma una lista enlazada simple en una lista doblemente enlazada, donde cada nodo tiene un puntero al siguiente y al anterior.

12. Implementar una lista circular:
    - Crea una lista donde el último nodo apunte al primero, permitiendo recorridos circulares.

Ejercicios con recursión:

13. Imprimir la lista de forma recursiva:
    - Implementa una función recursiva que imprima los elementos de la lista uno por uno.

14. Imprimir la lista al revés usando recursión:
    - Usando recursión, imprime los elementos desde el final hasta el principio.

15. Calcular la longitud de la lista de forma recursiva:
    - Implementa una función que devuelva la cantidad de nodos de la lista usando recursión.

16. Buscar un elemento de forma recursiva:
    - Implementa una búsqueda que use recursión para determinar si un valor está en la lista.

17. Insertar al final de forma recursiva:
    - Implementa la inserción de un nodo al final usando recursión.

18. Eliminar todos los nodos con un valor dado usando recursión:
    - Elimina de la lista todos los nodos que tengan un cierto valor.

19. Comparar dos listas recursivamente:
    - Verifica si dos listas son iguales (mismos valores y orden) usando recursión.

20. Clonar una lista usando recursión:
    - Crea una copia de la lista original utilizando una función recursiva.
*/
