/*
21. Usar punteros dobles para insertar nodos:
    - Implementa una función que recibe un puntero doble a la cabeza y un valor, e inserta un nodo al principio.

22. Implementar una pila usando lista enlazada:
    - Utiliza una lista enlazada para simular una pila con operaciones push y pop.

23. Implementar una cola usando lista enlazada:
    - Simula una cola (FIFO) con inserciones al final y extracciones desde el principio.

24. Calcular el promedio de los valores de la lista:
    - Recorre la lista y calcula el promedio de todos los valores almacenados.

25. Crear una lista desde un array dinámico:
    - Dado un array dinámico (creado con malloc/new), crea una lista enlazada con esos valores.

26. Contar cuántos elementos mayores a un número dado hay en la lista (recursivo o no).

27. Encontrar el valor máximo y mínimo de la lista de forma recursiva.

28. Liberar toda la memoria de la lista con una función recursiva.

29. Implementar una lista enlazada de estructuras más complejas:
    - Por ejemplo, una estructura Persona con nombre y edad. Crear una lista de personas.

30. Reordenar la lista alternando nodos desde el principio y el final:
    - Dada una lista 1→2→3→4→5→6, transformarla en 1→6→2→5→3→4.

*/